Chance/ add Fourer calibration

Now in the CYD we have 7 page for settings and callibration
On page 6 remobe the CAL Curent option and replace it with the labal MAN CAL
remove the logick behing the removed option completely

On the new page the folling should happen.

Show the 8 potitions like in the GPS COMPASS cal display
Make the derictions cliceble

if a direction is selected steer to that direction using the speed dir command with speed set to zero
if the compass has a offset it will vishely not point to the right direction.
With two buttons i want te be able to make correction steps of 1 degree (- and +)
if is the buoy is pointing to the right direction i want to store the current offest in the to fourer table by clicikng on a buttons

if i click on a other postion (one off the 8) repeat the procidure

start alwasy with the North position. But add a button. set as north for the default offest The compas can be mounted in a other direction
Remove that button (option) for other postitions

Do not change anything yet but make a proposal formatted in a new_man_foerier_cal_read.me
